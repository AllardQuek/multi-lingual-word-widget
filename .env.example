# Example environment variables for multi-lingual-word-widget
# Copy this file to .env.local and fill in real values (do NOT commit .env.local)

# Elastic Agent Builder tool endpoint
ELASTIC_API_URL=https://your-elasticsearch-endpoint.example.com/s/default/api/agent_builder/tools/_execute

# Base64 ApiKey for Elastic (format: id:api_key encoded as base64) — keep secret
ELASTIC_API_KEY=changeme_base64_api_key

# Optional: override tool id if you use a custom tool
ELASTIC_TOOL_ID=word.of.the.day.multilingual
